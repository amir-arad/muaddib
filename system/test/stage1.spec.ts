import {plan} from "./testkit/chai.spec";


describe('system', () => {
    describe('stage 1 - single vm, shallow plugins demo app', () => {

        it('load two plugins and have them all find and communicate with each other', plan(1, () => {

        }));

        xit('plugins as prototype and singleton', plan(1, () => {

        }));
    });
});
